{
  "name": "entrevista-campo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "node --test \"test/*.test.mjs\"",
    "test:ui": "node scripts/usabilidade.mjs",
    "validar": "node scripts/validar.mjs",
    "comparar": "node scripts/comparar-transcricao.mjs",
    "servir": "node scripts/servir.mjs",
    "deploy": "npm run build && npx wrangler pages deploy out --project-name=formula-de-impacto --commit-dirty=true"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "next": "^15.5.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
